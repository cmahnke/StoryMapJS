// Docs-only regeneration entry: never emitted (see vite.sitegen.config.ts,
// which builds with write:false). Exists only so Rollup has an input while
// the sitegen plugin regenerates public/ assets.
export default null;
